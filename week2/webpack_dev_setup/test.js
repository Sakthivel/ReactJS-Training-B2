// const add = require('./index.js');

// console.log(add(10,20));

// node 8.x wont support import & export so we have to use transpiling  babel

import add from './index.js';

console.log(add(10,20));